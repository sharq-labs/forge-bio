name: Spec Integrity

on:
  pull_request:
  push:
    branches:
      - main
      - "docs/**"

permissions:
  contents: read

jobs:
  spec-integrity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate scientific specs
        run: python scripts/check_specs.py
